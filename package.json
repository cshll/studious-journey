{
  "name": "my-dockerized-app",
  "version": "1.0.0",
  "description": "A multi-container Node, PHP, MySQL stack.",
  "scripts": {
    "start": "live-server --port=3000"
  },
  "dependencies": {},
  "devDependencies": {
    "live-server": "^1.2.2"
  }
}
